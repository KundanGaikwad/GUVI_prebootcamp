let strCaps = function(array){
    let result = [];
    result = array.map(current => current.toUpperCase());
    console.log(`Odd Numbers are ${result}`);
}

strCaps(["Kundan","Sweety","Parnika","Vicky","Deepak","Yogesh"]);